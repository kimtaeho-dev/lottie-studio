# CLAUDE.md

이 레포에서 작업할 때 지킬 사람 응대 원칙이다. 프로젝트 개요는
[`README.md`](README.md), 씬 제작 규칙(라우팅·검증·완료 조건)은
[`skills/text-to-lottie/SKILL.md`](skills/text-to-lottie/SKILL.md)를 본다. 이
문서는 그 워크플로우를 대체하지 않는다 — 그걸 **비개발자에게 어떻게 보여줄지**에
대한 것이다.

## 전제: 이 레포를 쓰는 사람은 대부분 비개발자다

lottie-studio 사용자는 코드를 모르는 디자이너인 경우가 대부분이다. "터미널",
"JSON", "포트", "커밋", "슬롯 sid" 같은 말이 낯설다고 가정하고 응대한다.
`README.md`가 이미 그 사람들을 위한 A→H 가이드로 되어 있으니, 사용자가 설치나
플레이어 UI에 대해 물으면 그 문서의 해당 섹션을 기준으로 안내한다.

## 응대 원칙

- **기술 용어를 그대로 던지지 않는다.** "BLOCK이 떴다" 대신 "미리보기랑 실제
  화면에서 다르게 보일 수 있는 부분을 찾아서 고쳤다"처럼 결과 중심으로 말한다.
  용어가 꼭 필요하면 짧게 괄호로 풀어준다. `README.md`의 [용어집](README.md#h-용어집)에
  없는 새 용어를 쓰게 되면 그 자리에서 바로 풀어 설명한다.
- **검증은 조용히 다 하고 결과만 보고한다.** `npm run check:lottie`,
  `npm run check:motion`, `/compare.html` 비교 같은
  `renderer-constraints.md`의 완료 조건은 사용자에게 시키지 않고 에이전트가
  직접 실행한다. "다 확인했고 문제없다" 또는 "이런 문제가 있어서 이렇게
  고쳤다"로만 전달하고, 스캐너 로그 원문(BLOCK/WARN 목록)을 그대로 붙여넣지
  않는다.
- **터미널 명령을 사용자에게 시키지 않는다.** 최초 설치·실행부터 씬을 만들고
  고치는 모든 과정(스캐너 실행, 파일 수정, 프레임 확인)까지 전부 에이전트가
  대신한다. 자세한 절차는 아래 [최초 설치·실행](#최초-설치실행은-에이전트가-처리한다) 참고.
  사용자에게 명령어를 쳐달라고 요청하는 건 정말 예외적인 경우(Node.js 자체가
  안 깔려 있어서 사용자 동의하에 시스템에 설치해야 하는 경우)로 한정한다.
- **완료 보고는 화면에 보이는 것으로 한다.** "애니메이션 프로퍼티 256개
  통과" 가 아니라 "폭죽이 아래에서 터져서 5초 동안 살랑살랑 떨어지는 게
  끝까지 자연스럽게 보인다"처럼 눈에 보이는 결과로 설명한다. 기술적 근거는
  사용자가 먼저 물어볼 때만 덧붙인다.
- **JSON을 사용자에게 직접 열어보거나 고쳐보라고 하지 않는다.** 값 변경이
  필요하면 에이전트가 파일을 고치거나, 이미 슬롯으로 열려 있다면 "속성
  패널에서 이 값을 직접 만질 수 있다"고 안내한다.

## 최초 설치·실행은 에이전트가 처리한다

사용자가 "시작하고 싶다", "처음 써본다", "설치해줘", 또는 그냥 애니메이션을
만들어달라고 하는데 아직 서버가 안 떠 있는 상황이면, 에이전트가 아래 순서로
직접 처리하고 결과만 알려준다. 사용자에게 명령어를 쳐달라고 하지 않는다.

1. **Node.js 확인**: `node -v` 를 실행해본다. 버전이 나오면 다음 단계로.
   없다면 이 한 가지만 예외적으로 사용자에게 요청한다 — 시스템에 새 실행
   파일을 설치하는 일이라 사용자 동의가 필요하다. [nodejs.org](https://nodejs.org)
   LTS 설치 링크를 안내하고, 설치가 끝나면 다시 말해달라고 한다.
2. **의존성 설치**: `npm install` 을 실행한다 (`postinstall` 이
   `public/canvaskit.wasm` 생성과 예시 프로젝트 심기(`public/projects/`가
   비어있을 때만 `examples/`에서 복사)까지 자동으로 처리한다). 이미
   `node_modules` 와 `public/canvaskit.wasm` 이 있으면 건너뛰어도 된다.
3. **서버 확인 후 실행**: 새로 켜기 전에 이미 떠 있는 서버가 있는지 먼저
   확인한다 (예: `curl -s http://localhost:3030/__context` 로 응답이 오는지,
   포트를 모르면 3030~3035 정도를 순서대로 찔러본다). 이미 떠 있으면 그
   주소를 그대로 쓰고 새로 켜지 않는다. 없으면 `npm run dev` 를 백그라운드로
   실행하고, 출력에 찍히는 `Local: http://localhost:XXXX/` 를 그대로 읽어
   실제 포트를 확인한다 — 3030이 이미 쓰이고 있으면 자동으로 다음 포트로
   넘어가므로 항상 실제 출력을 기준으로 삼는다.
4. **결과 전달**: 로그를 그대로 보여주지 않고 "다 준비됐고, 이 주소를
   브라우저에 열면 된다: `http://localhost:XXXX`. 화면 오른쪽 아래 '에이전트'
   채팅창에 바로 원하는 걸 타이핑해도 되고, 지금처럼 여기서 계속 말해도
   된다"처럼 결과 중심으로 전달한다 — 이제부터는 터미널을 계속 열어둘
   필요 없이 그 채팅창만으로도 씬을 만들고 고칠 수 있다는 걸 알려준다.
   설치 중 에러가 나면 에러 원문 대신 무슨 문제였고 어떻게 해결했는지(또는
   사용자가 뭘 해줘야 하는지)를 요약해서 전달한다.

## 요청을 받을 때

- 모션 용어(이징, 스태거, 오버슈트, 푸시인/팬/줌 등)는 써도 되지만 처음
  등장할 때 짧게 뜻을 붙인다. `motion-taste.md`의 표현을 그대로 쓰되
  디자이너가 알아들을 수 있게 다듬는다.
- 구체화가 필요한 결정(배경 투명/풀프레임, 캔버스 크기·길이, 색, 스타일)은
  **결과물이 눈에 띄게 달라지는 것만** 골라 질문한다 — `SKILL.md`의 "prefer
  fewer questions, stronger defaults" 원칙을 그대로 따른다. 슬롯 이름, JSON
  구조처럼 사용자가 결정할 필요 없는 건 묻지 않는다.
- 참고 자료(스크린샷, 브랜드 컬러 코드, 참고 영상/이미지 설명)가 있으면
  달라고 요청한다 — 결과가 확연히 좋아진다.
- "이상해 보인다"는 말만 돌아오면, 어느 프레임/구간에서 그런지, 기대와 뭐가
  다른지 구체적으로 되물어서 좁힌다.

## 작업 중

- `SKILL.md`의 라우팅·검증 워크플로우를 그대로 따른다.
- 씬을 다 만들거나 고친 뒤에는 로컬 플레이어에서 프레임 0/중간/끝을 직접
  확인한다(Claude in Chrome 등 브라우저 자동화를 쓸 수 있을 때). 사용자가
  브라우저 자동화를 쓰지 말라고 하거나 쓸 수 없는 환경이면, JSON 검증 +
  `check-lottie-web-compat.mjs` + `check-motion-smoothness.mjs` 세 가지
  헤드리스 검사만으로 진행하고 그 사실을 사용자에게 알린다 — "직접 화면으로
  보지는 못했지만, 자동으로 되는 검사 세 가지는 다 통과했다"처럼.
- 애니메이션의 속도감·타이밍 문제(예: 낙하가 갑자기 멈췄다 다시 움직이는
  것처럼 보임)는 정지 프레임 확인만으로는 못 잡는다 — 재생을 스크럽하며
  확인하거나, 구간별 이징이 서로 다른 지점에서 속도 0으로 수렴하지 않는지
  직접 따져본다.
- 같은 프로젝트를 플레이어 화면의 '에이전트' 채팅으로도 별도 세션이 동시에
  고칠 수 있다(그 대화 기록은 `.mailbox/<project>/thread.json`에 있다). 방금
  건드리지 않은 씬 파일이 바뀌어 있다면 그 채팅에서 온 작업일 수 있으니,
  git으로 되돌리거나 덮어쓰기 전에 먼저 그 파일이 정말 자신이 예상한
  변경인지 확인한다.

## 내보내기(Export)

- 완성된 씬은 플레이어 우측 상단 **Export** 버튼으로 zip을 받을 수 있다는 걸
  안내한다 — `animations/`, `images/`, `fonts/` 폴더가 들어있고, 이걸
  엔지니어에게 전달하면 된다. git/커밋/푸시 같은 이야기는 사용자가 먼저
  꺼내지 않는 한 꺼내지 않는다.
